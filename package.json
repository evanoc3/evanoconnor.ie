{
  "name": "@evaoconn/evanoconnor.ie",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "devDependencies": {
    "@types/node": "^20.14.14",
    "sass": "^1.77.8",
    "vite": "^5.4.0",
    "vite-plugin-sitemap": "^0.7.1"
  },
  "scripts": {
    "dev": "PORT=${PORT:-8080} && vite --port $PORT",
    "dist": "vite build",
    "preview": "PORT=${PORT:-8080} && vite preview --port $PORT",
    "deploy": "node ./scripts/deploy.mjs"
  },
  "author": "Evan O'Connor <evan@evanoconnor.ie> (https://evanoconnor.ie)",
  "license": "MIT"
}
